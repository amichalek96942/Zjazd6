Dana jest siatka znaków n×m oraz pojedynczy znak. Policz całkowitą liczbę jego wystąpień w siatce oraz podaj numer wiersza (0-indeksowany) z największą liczbą wystąpień.

Format wejścia:

Pierwsza linia: n m (1 ≤ n,m ≤ 50)
Kolejne n linii: po m znaków (bez spacji)
Ostatnia linia: znak do wyszukania
Format wyjścia:

Dwie linie: całkowita liczba wystąpień oraz indeks wiersza z maksimum.


#include <stdio.h>

int main() {
    int n = 0;
    int m = 0;

    scanf("%d %d", &n, &m);

    char tablica[n][m];

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            scanf(" %c", &tablica[i][j]);
        }
    }

    char check = '\0';
    scanf(" %c", &check);

    int wystepowanie_znaku[50];
    int suma_calkowita = 0;

    for (int i = 0; i < n; i++)
    {
        int licznik_wiersza = 0;
        for (int j = 0; j < m; j++)
        {
            if (check == tablica[i][j])
            {
                licznik_wiersza++;
                suma_calkowita++;
            }
        }
        wystepowanie_znaku[i] = licznik_wiersza;
    }

    int maksimum_wartosci = 0;
    int indeks_maksimum = 0;

    for (int i = 0; i < n; i++)
    {
        if (wystepowanie_znaku[i] > maksimum_wartosci)
        {
            maksimum_wartosci = wystepowanie_znaku[i];
            indeks_maksimum = i;
        }
    }

    printf("%d\n", suma_calkowita);
    printf("%d\n", indeks_maksimum);

    return 0;
}
