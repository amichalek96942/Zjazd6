Dany jest napis. Sprawdź, czy jest palindromem (czyta się tak samo od początku i od końca). Wypisz 'TAK' lub 'NIE'.

Format wejścia:

Jedna linia: łańcuch znaków bez spacji.
Format wyjścia:

Jedna linia: 'TAK' lub 'NIE'.



#include <stdio.h>

int main() {

    char napis[100] = {0};
    char pomocnicza[100] = {0};

    scanf("%s", napis);

    int number_of_characters = 0;

    for (int i = 0; napis[i] != '\0'; i++)
    {
        number_of_characters++;
    }

    for (int i = 0; i < number_of_characters; i++)
    {
        pomocnicza[i] = napis[number_of_characters - 1 - i];
    }

    pomocnicza[number_of_characters] = '\0';

    int jest_palindromem = 1;

    for (int i = 0; i < number_of_characters; i++)
    {
        if (napis[i] != pomocnicza[i])
        {
            jest_palindromem = 0;
            break;
        }
    }

    if (jest_palindromem)
        printf("TAK\n");
    else
        printf("NIE\n");

    return 0;
}
